const materials = ['Hydrogen', 'Helium', 'Lithium', 'Beryllium'];

console.log(materials.map((material) => material.length));
// Expected output: Array [8, 6, 7, 9]

/**
 * *materials은 배열[]을 값으로 가지는 변수로 선언되었다.
 * *콘솔에 기록해라 (materials.map 이라는 함수를.
 * *material.map 라는 함수 는 (material)이라는 함수를 매개변수 값으로 가지고 (material)은 material.length 값을 할당 받는다)
 */